{
  "agent_type": "workflow_agent",
  "name": "heartbeat-schedule",
  "description": "Sample workflow: fires every minute via the scheduler tick to demonstrate Schedule triggers.",
  "version": "0.1.0",
  "definition": {
    "id": "heartbeat-schedule",
    "steps": [
      {
        "id": "tick",
        "label": "Heartbeat tick",
        "kind": {
          "type": "checkpoint",
          "message": "💓 heartbeat fired — workflow scheduler is alive (source={input.source})"
        }
      }
    ],
    "entry_points": [
      {
        "id": "main",
        "label": "Heartbeat",
        "description": "Logs a heartbeat every minute via the scheduler tick",
        "starts_at": "tick",
        "preset_results": {},
        "required_inputs": [],
        "triggers": [
          {
            "type": "schedule",
            "cron": "0 * * * * *",
            "enabled": true,
            "input": { "source": "scheduler" }
          }
        ]
      }
    ]
  }
}
