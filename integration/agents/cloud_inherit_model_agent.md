---
name = "cloud_inherit_model_agent"
description = "Agent without model_settings — must inherit workspace default"
tools = ["final"]
context_size = 4000
---

You are a test agent. Reply with the literal single word "pong" via
`final({"result":"pong"})`. Do not add any other text.
