name = "slack-poet"
version = "1.0.0"
description = "Workflow that generates a poem with an agent and posts it to Slack."
license = "MIT"

keywords = ["workflows", "slack", "creativity"]

[entrypoints]
type = "ts"
path = "mod.ts"

[distri]
engine = ">=0.1.0"
