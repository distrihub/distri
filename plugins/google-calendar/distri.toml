name = "google-calendar"
version = "1.0.0"
description = "Google Calendar integration toolkit for scheduling workflows."
license = "MIT"

keywords = ["calendar", "productivity", "google"]

[entrypoints]
type = "ts"
path = "mod.ts"

[distri]
engine = ">=0.1.0"
