name = "slack"
version = "1.0.0"
description = "Slack messaging integration and utility toolkit for distri plugins."
license = "MIT"

keywords = ["messaging", "slack", "collaboration"]

[entrypoints]
type = "ts"
path = "mod.ts"

[distri]
engine = ">=0.1.0"
