name = "notion"
version = "1.0.0"
description = "Notion automation helpers for search and page creation."
license = "MIT"

keywords = ["knowledge", "notes", "notion"]

[entrypoints]
type = "ts"
path = "mod.ts"

[distri]
engine = ">=0.1.0"
