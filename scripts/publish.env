# Sourced by scripts/publish.sh — repo-specific overrides.
VERSION_CRATE="distri-cli"
BINARY_NAME="distri"
TARBALL_DIR="releases"
# BUILD_CMD / TEST_CMD use defaults: "make build-all && make release-tarballs" / "cargo test --workspace"
