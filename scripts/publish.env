# Sourced by scripts/publish.sh — repo-specific overrides.
VERSION_CRATE="distri-cli"
BINARY_NAME="distri"
TARBALL_DIR="releases"
# Linux-only build for VM (macOS builds are done locally separately)
BUILD_CMD="make build-linux build-linux-arm && make release-tarballs"
